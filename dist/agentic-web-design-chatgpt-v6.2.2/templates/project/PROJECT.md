# PROJECT — replace-me

Complete `brief.md` before approving G0. Keep this file as the short human-readable entry point: what is being made, for whom, and where the implementation lives.

Before G4 approval, set `implementation_root` in `project.config.json` to the real implementation directory relative to the OS repository root.
