---
name: web-design-capabilities
description: Route high-end web design work through a project-specific direction, optional design knowledge, interface polish, motion craft and final interface audit. Use during Agentic Web Design OS stages G2–G4; do not use it to replace strategy, content, ownership or technology selection.
---

# Web Design Capabilities

Use the capability matching the active stage. Project evidence and approved decisions override every external preference.

For any material choice, follow `docs/methods/material-decisions.md` inside the active owner artifact. External capabilities may supply or challenge candidates, but they cannot select one. Add only cross-phase or expensive-to-reverse outcomes to `decision-log.md`.

## Routing

- Creative direction or visual identity: read [references/direction.md](references/direction.md).
- Targeted style, typography, UX or stack lookup: read [references/lookup.md](references/lookup.md).
- UI refinement or design/build polish review: read [references/polish.md](references/polish.md).
- Material motion or interaction work: read [references/motion.md](references/motion.md).
- Final build audit: read [references/audit.md](references/audit.md).
- Explicitly approved immersive experience only: read [references/immersive.md](references/immersive.md).
- Landing-flow comparison: read [references/structure-challenger.md](references/structure-challenger.md).
- Reference video or HTML interaction: read [references/interaction-reference.md](references/interaction-reference.md).
- `GENERIC | FLAT | SAFE | OVERDESIGNED | WEAK_HIERARCHY | INTERCHANGEABLE` review finding: read [references/craft-correction.md](references/craft-correction.md).
- GSAP already selected by technology decision: read [references/runtime-motion.md](references/runtime-motion.md).

Do not load every reference. Record each material use under `## Design capability log` in the artifact owned by the active stage.

## Boundaries

- One primary direction capability; at most one challenger per exploration round.
- A lookup returns candidates, not final identity decisions.
- Polish, motion and audit findings return to the causal owner.
- “Premium” never activates immersive patterns by itself.
- External skills cannot impose frameworks, libraries, fonts, effects or brand choices.
- GSAP knowledge activates only after runtime selection; Impeccable activates only from a concrete 07 finding; MengTo activates only from temporal/HTML reference evidence.
- A capability log records tool use; it is not evidence that alternatives were compared. Keep comparison evidence in the active owner artifact.
- Discovered material has no automatic authority; the active owner decides how it affects the project.
- Every landing carries a substantial project-specific visual unless the user explicitly requests text only. Missing media triggers production.
- Each direction visibly compares a static scene with simple and expressive versions of one project-specific mechanism. The selected result is tested and delivered with evidence.
- Revalidate external source and license according to `config/design-capabilities.json`; use the local fallback if unavailable or incompatible.
